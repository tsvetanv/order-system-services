spring:
  application:
    name: order-database
  datasource:
    url: jdbc:postgresql://localhost:5432/orders
    username: orders
    password: orders

  flyway:
    enabled: true
    locations: classpath:db/migration